import  main  from '../index.js'

export default function generateFromUser(symbol:string,startDate:string, endDate:string, capital: number, activeRules: String[]){

    try {
        const 
        
        
    } catch (error) {
        
    }

}